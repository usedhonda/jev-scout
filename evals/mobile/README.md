# Offline notification organizer

The app promises offline-only processing. Notifications have `app_id`, `received_at` and optional text. Current ordering is newest first. No backend, cloud consent, or notification export exists. A proposed feature request asks for grouping related notifications, not for sending them to a server.
