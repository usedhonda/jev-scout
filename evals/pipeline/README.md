# Document search pipeline

Input is an in-memory list of public text documents. The search returns five keyword-overlap candidates. Users report that paraphrases are missed; no labeled query set or latency objective exists yet. Relevant passages can be extracted before sending any data to another service.
