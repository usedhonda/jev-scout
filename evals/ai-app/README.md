# Assistant application

`reply` first chooses an existing tool, then generates a prose answer. The two calls serve different purposes. No accuracy or request-cost measurements have been collected. Tool execution must enforce its own permissions.
